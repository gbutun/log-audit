apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: log-audit-data
  namespace: log-audit
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 10Gi
